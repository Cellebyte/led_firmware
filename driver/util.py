def sub_tuple(a, b):
    return tuple(map(lambda x, y: x - y, a, b))


def add_tuple(a, b):
    return tuple(map(lambda x, y: x + y, a, b))


def multiply_tuple(a, b):
    return tuple(map(lambda x, y: x * y, a, b))
